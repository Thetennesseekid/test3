#!groovy
//test
properties([disableConcurrentBuilds()])

pipeline {
	agent any

	stages {
		stage("create docker image") {
		steps {
			echo "======Start building imnage======"
// 				dir('test3/src/src1') {
					bat(script: "cd src/src1 && dir && docker build . -t test && docker run test ")
					bat(script: "cd src/src2 && dir && docker build . -t test2 && docker run test2")
// 				}
			}
		}
	}
}
